-- CONFIGURATION

-- Activate on these AI modes. Valid keys are "off", "on", "combat",
-- "patrol", and "fleetmove".
ActivateWhen = {
--   on = true,
   combat = true,
}

VaryThrottle = true
ThrottleSettings = { 1, .5 }

HeadingDelay = 2000
ThrottleDelay = 400
